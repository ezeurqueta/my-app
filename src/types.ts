export interface Token {
    token: string;
    expiresIn: string;
    isAdmin: boolean;
  }
  export interface Movie {
    id: number
    titulo: string
    fechaEstreno: string
    poster: string
  }
